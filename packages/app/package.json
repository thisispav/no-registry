{
  "name": "@noregistry/app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "No Registry static site (original single-file HTML/CSS)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
